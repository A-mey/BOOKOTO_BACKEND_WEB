import { Product } from "../types/product.type"

export class ProductMockDao {

    constructor () {}

    getAllProductsDao = async (from: number, to: number) : Promise<Product> => {
        try {
            const products = 
        } catch (error: unknown) {
            
        }
    }
}